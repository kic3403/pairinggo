# 페어링GO

술↔음식 양방향 페어링 추천 → (Stage 2) 전통주 구매 → (Stage 3) 식당 예약.
**플랫폼: 반응형 웹 우선 → 앱스토어·구글플레이. 앱인토스는 후순위** (2026-09-11 변경, 근거·순서는 docs/13). 플랫폼에 관해서는 docs/13이 docs/05보다 우선한다.
기획·설계 문서는 docs/ (00~24). 데이터 운영은 docs/11, 앱 점검 결과·남은 과제는 docs/12·15, **최신 보완점 진단은 docs/20**(2026-09-15, 실측), 이전 진단 docs/18(2026-09-13), 경쟁 서비스(데일리샷·캐치테이블) 분석과 적용안은 docs/19.
현재: **공개 웹 배포됨 — https://pairinggo.vercel.app** (docs/14, Vercel·GitHub 연결, `git push`로 자동 배포) · 검색·구매·주변 판매점·회원(이메일+소셜)·하트·마이페이지 완료(docs/13) · 데이터 수집 도구 완료(docs/11) · 카카오 식당·위치 완료(docs/10).
- 배포 사이트를 확인할 때 클로드 앱의 미리보기 창은 `/_next/static` 자산을 막아 스타일 없이 보인다 — 실제 브라우저(Claude in Chrome 또는 사용자 크롬)로 본다.
다음: 유입 만들기 + 사업자·통신판매업 신고 병행 → PWA → **구매 기능(전통주 중개 — 설계 docs/22, 2026-09-20 사용자 결정: 판매자는 양조장, 우리는 통신판매중개자·수단제공자. 직접 매입 판매는 보류)** → 스토어 출시.
- "많이 찾는 전통주" 순위 = 인스타·유튜브·네이버 블로그·구글 블로그 최근 30일 언급량 채널별 정규화 평균, 매일 00:00 KST 크론 `/api/cron/mentions` → `drinks.trend`(docs/11 §5-2, 점수 로직 `packages/shared/src/trend.ts`). 인스타는 수동 CSV(`mentions:insta`). **채널마다 무료 할당량이 달라 회전 수집한다**(2026-09-22 수정): 네이버는 매일 전량, 유튜브 75종·구글 90종씩 **가장 오래 세지 않은 술부터**(`shared/trend.ts pickByStaleness`, 한 바퀴 7일) — 점수는 창(`LOOKBACK` 12일) 안의 마지막 값을 쓴다. 옛 방식(하루 절반씩)은 절반이 할당량보다 커서 앞쪽만 세고 뒤쪽은 영영 못 셌다(실측 유튜브 196/514). 기록 조회는 1,000행에서 잘리므로 끝까지 페이지로 받는다. `?probe=1`은 밖으로 나가지 않고 키·수집 상태만 돌려준다. **구글 채널은 쓸 수 없다**(2026-09-22 확인): 키·검색엔진(cx)을 만들고 프로젝트 3개(조직 소속·조직 없음·옛 프로젝트)와 키 4개로 시험했지만 모두 `403 PERMISSION_DENIED — This project does not have the access to Custom Search JSON API`. 구글이 계정 단위로 Custom Search JSON API를 열어 주지 않는 상태라 설정으로 못 푼다. 키가 있으면 크론이 시도했다가 첫 호출에서 멈춘다(하루 1회 실패 기록). 쓰려면 유료 대행(Serper.dev 등)이나 다음(카카오) 블로그 채널 추가가 필요 — **사용자 결정 2026-09-22: 보류**.
- 급상승·월간 리포트(docs/19 §3-1): 크론이 7일 전 순위를 함께 계산해 `trend.prev_rank/delta`, 배지는 `trend_meta.compared_to`가 있을 때만. `/report`는 `lib/report.ts`가 최근 30일 데이터로 조립(1시간 캐시), "글로 복사"로 SNS 글 생성.
- 휴대폰 레이아웃(767px 이하, docs/19 §5): 하단 탭바 `MobileTabBar`(헤더 메뉴 숨김), 술·음식 상세는 탭바 대신 하단 고정 버튼 `DetailActionBar`(♡ 저장 · 목록 · 구매/맛집). 페어링 카드는 전체 탭에서 10장까지 보이고 더 보기(`PickTabs`), **카드의 페어링 포인트·주의는 문장이 아니라 짧은 칩**(`pairing/summary.ts shortPoint` — 생성 문형 19가지를 2~6자 라벨로, 원문은 툴팁, 긴 설명은 '자세히', 2026-09-26 사용자 요청), 상세 상단 맛 프로필 막대 `ProfileBars`, **페어링 카드 안 맛 프로필은 작은 세로 막대 그래프** `ProfileChart`(2026-09-24 사용자 요청 — 글줄 대신 높낮이가 먼저 보이게. 축·순서는 `profileAxes`로 상세와 같고, 읽어 주는 기기에는 `profileLine` 한 줄로 들려준다). **막대 색도 종류를 따른다** — 술 주황·음식 남색(`.pchart.d/.f`·`.pbars.d/.f`), 그래서 술 상세는 위쪽(그 술)이 주황·카드 안(어울리는 음식)이 남색으로 엇갈린다. **한 화면에 같은 것을 두 번 두지 않는다**(2026-09-23): 검색 화면은 지역 고르개를 검색창에서 빼고 칩만 쓰며(`SearchBox regionPicker={false}`), 검색어가 있으면 칩을 한 줄로 접는다(`RegionTabs collapsible`, [바꾸기]로 펼침). 술 상세에서 하단 고정 막대와 겹치는 본문 버튼(공식몰·저장)은 `.bar-dup`로 휴대폰에서만 감춘다. 설치 안내 `InstallPrompt`는 **홈에서만** 뜨고 15초 뒤 저절로 사라진다(목록·검색을 가리지 않게).
- 구조화 데이터(docs/20 P3-4, 2026-09-23): 규칙은 `shared/seo/jsonld.ts`(`drinkProduct`·`breadcrumb`·`itemList`·`website`), 심기는 `_components/JsonLd.tsx`. 술 상세 = Product(도수·종류·지역은 additionalProperty, 양조장 = brand, 수상 = award) + 빵부스러기, **입점 상품이 있을 때만 offers**(가장 싼 값·재고·판매자 — 우리는 중개자라 판매자를 밝힌다). 음식 상세·목록은 빵부스러기 + ItemList(30개까지), 홈은 WebSite + 사이트 안 검색. 값이 없는 항목은 키를 아예 넣지 않고(구글이 오류로 잡는다), `jsonLdScript()`가 `<`·`>`·`&`를 유니코드로 바꿔 `</script>`가 태그를 닫지 못하게 한다.
- 공유(docs/20 P0-1·2): 파비콘 `app/icon.tsx`·`apple-icon.tsx`, 링크 미리보기 그림 `app/opengraph-image.tsx`(홈)·`drinks/[slug]`·`foods/[slug]`의 `opengraph-image.tsx`(이름 + 어울리는 것 3개, 한글 폰트는 `lib/og.tsx`가 Google Fonts에서 글자만 받음). 홈처럼 openGraph를 직접 적는 페이지는 `images`를 명시해야 그림이 붙는다. 공유 버튼 `ShareButton`(카카오 SDK → 기기 공유창 → 복사, `share` 이벤트) — 카톡 카드는 `NEXT_PUBLIC_KAKAO_JS_KEY` + 카카오 콘솔 JS SDK 도메인 등록이 있어야 한다.
- PWA(docs/20 P1-3): `app/manifest.ts`·`icon-192.png`·`icon-512.png` 라우트·`public/sw.js`(캐시 없음, 설치 조건용)·`InstallPrompt`(안드로이드 설치창 / 아이폰 안내, 7일 닫힘). 식당 결과는 평점순 위에 관련도(`shared/places.ts placeRelevance`: 이름·분류에 음식 이름/키워드 → 같은 계열 → 다른 계열)로 다시 묶는다.
- **주종 확장 + 공통 가격·용량 필터**(2026-09-24, docs/23): `drinks.kind` = trad·whisky·sake·wine(0035, 기존 행은 trad), 분류·국가·주종별 속성 정의는 `shared/catalog/kinds.ts` 한 곳(표시명 ≠ DB 식별자 — `category`는 식별자, URL은 `Subtype.id`), 규격 `drink_specs`(용량 mL null=미확인·0 금지, 세트는 pack=set)·참고가격 `drink_prices`(>0, 유형·출처·확인일, 옛 값은 valid=false). `/drinks`는 **술 목록**: 주종 탭 → 세부 칩(`SubtypeChips`) → `FilterBar`([가격][용량][음식][전체 필터]) → `FilterSheet`(편집≠적용, 결과 수는 `/api/v1/drinks/filter?count=1`) → `DrinkCard`. 규칙은 `shared/catalog/filter-url.ts`(URL·요약·칩·슬라이더, 클라이언트 공용)·`filter.ts`(엔진 — **같은 한 병 규격에서 가격·용량 동시 충족**, 조건 없으면 미확인도 보임, 결과 수 = 술 수, 가격 없는 술은 가격순 마지막). `Range.max null = 상한 없음`(URL에서 키 생략). 상세는 `SpecPicker`(?spec=)·`KindFacts`. **위스키·사케·와인은 온라인 구매 안내 없음**(`onlineSellable` 전통주만). 데모 12종은 `is_demo`라 공개 카탈로그·번들·sitemap에서 빠지고 `SHOW_DEMO=1`(로컬 `.env.local`)일 때만 보인다 — 실데이터가 오면 `delete from drinks where is_demo`. 입력은 `add-drinks`(kind·attrs·specs) · `specs-import`(템플릿 '규격·가격' 시트) · 어드민 `/admin/drinks`. 가격은 **한 병 참고가격, 배송비·쿠폰 제외, 확인된 값만**(추정 금지). 헤더·탭바 '전통주' → '주류'(2026-09-25). 가격 슬라이더 기본 눈금 0~1,000,000원(오른쪽 끝 = 상한 없음). **외부 평점** `attrs.ext_rating`(출처·점수·척도·확인일, `cleanExtRating`)은 허용된 출처만 — **Vivino는 약관(자동 수집·상업적 이용 금지)상 쓰지 않는다**(2026-09-25 확인, 제휴 계약 전까지). 데모 술 이미지는 `public/demo/*.svg`(가상). **파트너 술 사진 폴백**(2026-09-25): 파트너 `/store` 술 표에 올린 사진(`place_info.drink_items[].img`)은 이름이 같은 카탈로그 술에 **공식 사진이 없을 때** 카드·검색 결과·상세 사진으로 쓴다(`shared/catalog/partner-images.ts partnerImages` — 승인 매장만, 그 술을 빚은 양조장 파트너 우선, 출처 "매장 이름 제공"; 웹 `lib/catalog.ts`·`export`가 같은 규칙). 파트너 술 표에는 **종류 선택**(`DrinkItem.category`, `categoryOptions()` 주종별 묶음)이 있어 손님 메뉴판에 종류가 보인다.
- **홈 재구성 + 시작 화면**(2026-09-25 사용자 요청, 캐치테이블·데일리샷 구조 참고 — docs/24): 홈 = 한 줄 소개 → `HomeTabs`(홈·전통주·위스키·사케·와인·음식·수상·핫한 페어링) → `HomeBanners`(scroll-snap 카드 캐러셀, "n/N 전체 ›" → `/events`) → `QuickMenu` 5열×2줄 → `FlowTiles` 4장(술로 찾기·음식으로 찾기·식당 예약·양조장 방문) → `PartnerRow`(승인 파트너 사진 동그라미) → `TrendRow`(정지 카드 — 흘러가는 마키는 뺐다) → 음식 칩 → 회원 추천 → 안주 → `TriedPill`(로그인 회원 "먹어봤나요? N", `/api/tried`). 히어로 문장·숫자·큰 버튼은 뺐다. 휴대폰 헤더도 전폭 검색창. **배너**는 `home_banners`(0036) + `shared/home/banners.ts`(`cleanBanner`·`activeBanners`·`bannerCards` — 리포트 자동 카드 맨 앞, 파트너 카드는 매장 정보로 자동 문구, 2장 미만이면 상시 카드) + 어드민 `/admin/banners`(저장 시 `revalidatePath("/")`). 링크는 사이트 안 주소만, 사진은 우리 저장소만. **시작 화면** `Splash`("맛있는 술엔 맛있는 음식", 1.2초 후 페이드) — 한 탭에 한 번(`sessionStorage.pg_splash`, layout의 인라인 스크립트가 `html.no-splash`로 첫 그리기 전에 감춤), 새로고침·앱 라우터 이동엔 안 뜸, 어드민·파트너 없음. **상세 정리**(docs/24 §6): 술·음식 상세는 머리 카드 `.dhead`(사진·이름·분류·태그·저장/공유) → 설명 → 규격·구매(판매점 찾기는 `details.fold`로 접음, 온라인 불가 주류만 펼침) → `.dinfo`(맛 프로필·주종 정보 나란히) → 페어링(등급 설명 접기) → 옆 칸 '관련 술' 한 칸. '데이터' 칸은 없앴다.
- **술 평가 + 파트너 페어링**(2026-09-26, docs/25): ① 술 단위 **별점·한줄평** `drink_reviews`(0038, 회원당 술 하나에 하나, `shared/pairing/drink-review.ts` — 140자·링크 금지·하루 20개, **평균은 3명부터** `DRINK_REVIEW_MIN_N`), API `/api/drink-reviews`(getToken), 상세 머리 카드 "★ 4.3 · 회원 평가 N명" 줄 + 페어링 카드 아래 `DrinkReviews` 칸, 3명 이상이면 JSON-LD `aggregateRating`, 어드민 `/admin/drink-reviews`(숨김·삭제). 근거 점수·먹어봤어요(조합 평가)와 섞지 않는다. ② **양조장 파트너가 직접 넣는 페어링** `partner_pairings`(0038) — 파트너 앱 `/pairings` 탭(양조장만, `merchants.brewery`의 술에만, 술당 8개) → `packages/server/partner-pairings.ts`가 `pairings`에 **official 90**으로 만들거나 올리고(`shared applyPartnerPairing` — 이미 official·sommelier면 유지) 근거 줄 "○○ 제공"을 붙인 뒤 `catalog_meta.version`을 올린다. 지우면 우리가 만든 행은 삭제, 아니면 `prev`로 되돌림.
- **재방문 알림(웹 푸시)**(2026-09-26, docs/25 §7): 주간 소식 크론 `/api/cron/weekly`(월 09:00 KST, `?dry=1`) — `lib/push-digest.ts weeklyRun`이 알림 켠 회원마다 `shared/push-digest.ts weeklyDigest`(저장한 술 새 페어링·상품 → 급상승 → 새 술 → 먹어봤나요?, 내용 없으면 안 보냄, 6일 간격 `users.weekly_push_at`). 활동 소식은 즉시(`activityPush`: 요청 처리·내 추천 하트, 설정 `users.push_pref`). 마이페이지 `PushSettings`, `GET/PATCH /api/push/pref`, `POST /api/push/test`.
- **헤더 최근 본 · 알림**(2026-09-26, docs/25 §6): `HeaderIcons` — 최근 본(매장·술·음식 탭, 기기 localStorage `pg_recent`, `RecentTrack`이 상세에서 기록, 서버 저장 없음) · 알림(공지 = `notices` 0040·어드민 `/admin/notices`, 활동 = 요청 처리·내 추천 하트·예약·주문을 `lib/notifications.ts myActivity`가 모음, `/api/notifications`). 배지 = 읽지 않은 수(공지는 기기 `pg_notice_seen`, 활동은 `users.notif_seen_at`), 빨강 `--like`. 규칙 `shared/notifications.ts`.
- **못 찾는 검색 입구**(2026-09-26, docs/25 §5): 검색 화면에 **라벨 사진으로 찾기** `LabelSearch` → `/api/v1/label-read`(`packages/server/label-read.ts` Claude 구조화 출력 → `lib/label-read.ts`가 shared `search`로 대조, 사진 저장 안 함, IP 분당 4·하루 300) + 빈 결과에 **추가 요청** `DrinkRequestForm` → `drink_requests`(0039, 규칙 `shared/drink-request.ts`, 회원 하루 10·비회원 분당 5). 어드민 `/admin/wanted` 맨 위 "회원 요청"에서 등록됨(카탈로그 술 선택)·보류 → 마이페이지 "내가 요청한 술". 열린 요청은 대기열 source `request`(무게 4).
- **상세 머리 카드 사진 칸**(2026-09-26, docs/24 §8): 술·음식 상세 `.dhead` 오른쪽에 `DetailMedia` — 사진이 있으면 사진+출처, 없으면 주종·분류 색 타일("사진 준비 중")로 자리를 지킨다(휴대폰 96px·640px부터 160px, 3:4). 음식 사진은 `foods.image_url·image_credit`(0037) → `Food.image`, 어드민 `/admin/foods`에서 주소로 입력, 술 공식 사진은 `/admin/drinks/[id]` 사진 칸. 주소 규칙 `shared/catalog/images.ts cleanCatalogImage`(https 또는 `/…`만). 사용 허락을 받은 사진만(더술닷컴 사진 금지).
- 홈 아이콘 메뉴(`QuickMenu`) — 기획 화면 `/michelin`(최근 3년, docs/17) · `/awards`(수상 전통주 — 홈 메뉴는 **우리술품평회 수상**(`?c=fair`)·**대한민국 주류대상**(`?c=kla`) 두 칸, 2026-09-24 사용자 요청) · `/hot`(최근 30일 조합, `lib/hot.ts`). 새 기능은 여기에 한 칸씩.
- **검색 결과 색**(2026-09-24 사용자 결정): 술은 로고 **주황**(`.badge.d`·`.sbox-pop .t-drink`), 음식은 로고 **남색**(기본 `.badge`·`.t-food`), 종류·지역·양조장·식당은 회색. 줄 전체가 같은 계열 — 이름은 `--food-ink`/`--navy-ink`, 딸린 글자는 `color-mix`로 `--ink2`를 섞어 한 톤 누른다(`.row.d`·`.row.f`, 흰 바탕 대비 5.4:1·6.5:1). **하트(찜)는 어느 화면에서나 빨강**(2026-09-24 사용자 결정, `--like`·`--like-ink`·`--like-soft`) — 술 주황·음식 남색과 섞이지 않게 기능 색을 따로 둔다(`.heart`·`.heart-btn.on`·`.pk-heart.on`). 파트너 인증 도장은 업종 기준이라 반대 색을 쓴다(양조장 남색·식당 주황, `PARTNER_KIND_TONE`).
- 목록 정렬(2026-09-14 사용자 결정): `/drinks`·`/foods` 목록은 가나다순(`byKoName`), 식당 목록은 평점 높은 순(`sortByRating`, 평점 없는 곳은 거리순으로 뒤). `/foods`는 대분류(`shared/food-groups.ts` FOOD_GROUPS: 한식·양식·중식·일식·안주·간식·디저트) 탭 → 소분류 칩(?group=&category=). 새 음식 category는 FOOD_GROUPS에 넣는다(테스트가 잡는다).
- 식당 평점: 구글 지도 평점만(Places API (New) Text Search, `GOOGLE_PLACES_KEY` 서버 전용, 2026-09-14). 카카오 목록 앞 12곳을 이름+좌표로 대조(`shared/place-rating.ts`, 150m·리뷰 5개 이상), `place_ratings`(0018)에 30일 캐시(구글 정책 상한). 평점 필드는 Enterprise 등급(월 1,000회 무료) — 하루 150회 상한(`lib/google-places.ts`). 카카오·네이버 별점은 API 없음·수집 금지. 화면엔 "Google" 표기 필수.
- 식당 편의 정보(2026-09-17): 식당 이름 옆 칩 `주차 무료·유료·발레·노상·불가 / 단체 가능 / 예약 가능` — 구글 Places의 parkingOptions·goodForGroups·reservable을 평점과 **같은 호출**에서 받아 `place_ratings.amenities`(0020)에 30일 캐시(`shared/place-rating.ts` `amenitiesFromGoogle`·`amenityChips`). 이 필드들은 Atmosphere 등급이라 호출 요금이 Enterprise + Atmosphere로 오른다(월 1,000회 무료, 하루 150회 상한 그대로). 적혀 있지 않은 것은 표시하지 않는다(실측: 강남 12곳 중 주차 2·단체 4·예약 4, 대전 12곳 중 주차 0). **콜키지·룸은 구글·카카오 어디에도 없다** — 회원 제보·제휴 식당 입력으로 따로(네이버 플레이스·캐치테이블 수집 금지).
- 운영자 식당 정보(2026-09-17 사용자 결정): 콜키지·룸·메뉴·취급 술은 어떤 공개 API에도 없고 네이버 지도·캐치테이블 화면 수집은 약관 위반 → **운영자가 전화·방문으로 확인해 `/admin/places`에 직접 입력**(카카오에서 식당 찾기 → 콜키지·룸·주차 + 메모, 취급 전통주·대표 메뉴는 카탈로그에서 선택). 표 `place_info`(0021, 카카오 장소 id 기준, source operator|partner — 제휴 식당 직접 입력도 같은 표), 규칙 `shared/place-info.ts`(`cleanPlaceInfo`·`placeChips`·`verifiedFirst`), 서버 `lib/place-info.ts`. 식당 검색 API가 매번 붙여 **확인된 식당을 목록 맨 앞**에, 이름 옆 칩(운영자 확인 = 남색, 구글 = 회색)과 "운영자 확인 2026.09 · 전통주 … · 메뉴 …" 상자로 보여 준다(주차는 운영자 값이 구글 값을 덮음). 식당 목록 API는 10분 캐시라 반영에 최대 10분. 회원 제보는 아직 없음(붙이면 운영자 값 아래 순위). 술·메뉴는 입력칸 둘("술 종류"·"메뉴")에 이름을 적고 **추가** 버튼으로 하나씩(0022) — 카탈로그 이름과 같으면 id로 연결돼 화면에서 그 페이지로 이어지고, 없으면 적은 이름 그대로(`addListItem`, `drink_names`·`menu_names`). **대표 번호 `contact_phone`·운영 메모는 운영자 전용** — `PlaceInfo`(공개 타입)에 넣지 않고 어드민 화면에만 내려 준다. 네이버 지도 링크 `naver_url`은 https + naver.com·naver.me만(`cleanNaverUrl`), 공개 카드에 "네이버 지도 ↗"로 보인다(데이터는 가져오지 않고 링크만). **메뉴판 사진으로 자동 입력**(2026-09-17): 사진(최대 4장)을 브라우저에서 긴 변 1,600px JPEG로 줄여 `/admin/api/places/menu-read`로 보내면 `lib/menu-read.ts`가 Claude(`claude-opus-5`, 구조화 출력 zod, `fallbacks: "default"`, 카탈로그 이름 목록은 system에 넣어 캐시)로 술·음식을 읽고, `shared mergeMenuRead`가 카탈로그와 대조해 입력칸에 **더하기만** 한다(지우지 않음, 지어낸 카탈로그 이름은 연결 안 됨). 사진은 저장하지 않는다. `ANTHROPIC_API_KEY`(서버 전용, `.env.local`·Vercel)가 없으면 그 칸만 "꺼짐".
- **식당 예약 + 페어링GO 파트너 앱**(2026-09-18, docs/21): 사장님은 `apps/partner`(PWA, Vercel 별도 프로젝트, 서명 쿠키 `pgo_partner`)에서 가입 신청 → `/admin/partners` 승인 → 영업시간·정원 정하고 예약 받기 켬. 예약은 **즉시 확정**(수락 단계 없음), 정원 판정은 DB 함수 `reserve()`(매장 설정 행 잠금), 전이는 `reservation_transition()` — 규칙은 `shared/reservation/`(한국 시간 기준). 손님은 `/reserve/[카카오id]`(로그인 + 휴대폰 문자 인증 + 예약마다 매장 제공 동의), `/my/reservations`(방문 1시간 전까지 취소). 매장 정보 수정은 `place_info`(source partner)에 바로 반영 + `merchant_changes` 이력(운영자 되돌리기, 운영자 전용 값은 유지). 알림은 `packages/server/notify`(웹 푸시 + 솔라피 알림톡, 저장 뒤 발송·실패는 기록만), 아침 크론 `/api/cron/reservations`가 당일 안내·보관 기한 정리. 솔라피 키 전에는 운영에서 예약 불가(개발은 인증번호를 서버 로그에). 손님 번호는 방문 90일 뒤 파트너 화면에서 가린다. **고른 조합을 파는 식당 먼저**(2026-09-19 사용자 요청): 술 화면 페어링 카드·/hot 맛집 링크가 음식 화면으로 `?d=술id`를 들고 가고, 맛집(`NearbyPlaces`)은 그 술을 읽어(없으면 어울리는 술 5개 칩으로 고르기) `/api/v1/places/restaurants?drink=`로 부른다. 서버가 확인 정보(place_info 취급 술·메뉴·메뉴판·콜키지)로 `shared/place-match.ts placeMatch` 순위를 매긴다 — 90 그 술+같은 음식 · 80 그 술+비슷한 음식 · **70 같은 음식+콜키지 가능(고른 술 가져가기)** · 60 그 술만 · 50 비슷한 음식+콜키지 · 40/30/25 같은 종류 술 · 20/10 음식만(음식 별칭은 비슷한 음식으로만). 같은 순위는 **콜키지 싼 곳부터**(`corkageFee`가 `corkage_note`에서 병당 값을 읽음 — "병당 1만원"·"전통주 무료"=0, 무료→값→값 모름→콜키지 없음, 파트너 매장 정보 화면에 금액 적기 안내). `matchFirst`로 맨 앞에, 카드 맨 위에 "딱 맞는 곳 · 한산소곡주 · 해물파전 함께 팔아요" 줄. 카카오 검색에 안 걸린 주변 확인 식당(`lib/place-info.ts nearbyPlaceInfo`, 반경 안 좌표)도 그 술·그 음식을 팔면 5곳까지 더한다. 예약 링크에 술도 넘긴다. 메뉴 정보가 없는 일반 식당은 기존 순서(평점·관련도). **매장 상세 `/places/[카카오id]?n=이름`**(2026-09-19, 모든 식당): 카카오는 id로 다시 찾는 API가 없어 파트너(merchants) → place_info → `?n=` 이름으로 카카오 검색해 같은 id → 리뷰에 남은 이름 순으로 찾는다(`lib/place-detail.ts`, 메타데이터·본문은 React `cache`로 한 번). 식당 카드 이름·검색창 식당 제안이 여기로. 파트너 매장은 **대표 사진 최대 10장**(`place_info.photos` 0028, 파트너 `/store` "대표 사진", 메뉴 사진과 같은 menu-photos 버킷·`cleanStorePhotos`)·메뉴판·예약. 평점은 둘로 나눠 표시 — 페어링GO(방문 인증 리뷰 평균) · Google. **방문 인증 리뷰**(사용자 결정 A — 아무나 쓰면 조작·별점 테러로 신뢰가 떨어져서): 별점 5 + 글 10~500자 + 사진 5장(`review-photos/{회원id}/`), **휴대폰 인증 회원 + 방문 인증**(① 이 매장 예약을 방문 완료 ② 영수증 사진 — `packages/server/receipt-read.ts` Claude가 상호·날짜·금액·승인번호를 읽고 `shared/reviews.ts receiptProblem`(상호 포함 관계·전화·사업자번호 중 하나 + 최근 30일), 사진은 저장 안 함, 30분짜리 서명 표) — 방문 한 번에 하나(`reservation_id`·`receipt_hash` 고유), 하루 리뷰 5개·영수증 읽기 5번, 파트너 번호로 인증한 회원은 자기 매장 불가, 신고 3건이면 자동 숨김 → 어드민 `/admin/place-reviews`(복구·삭제, 권리침해 임시조치 30일). 표 `place_reviews`·`review_reports`·`receipt_reads`(0028), 서버 `lib/reviews.ts`, 화면 `places/[kakaoId]/ReviewList`·`review/ReviewForm`. 공개 목록엔 닉네임만. 아침 크론이 리뷰에 안 붙은 사진(하루)·영수증 기록(7일) 정리. 방침에 리뷰 수집·공개·Anthropic 국외 이전, 약관 제8조의2 → 동의 버전 2026-09-19.3. **매장 직접 입력**(2026-09-19): 가입 때 카카오맵 검색에 안 나오는 매장은 상호·주소·전화(선택)를 직접 적어 신청(`shared cleanManualPlace`) → `merchants.kakao_place_id`에 표시 id `manual-…`(`isManualPlaceId`), 같은 상호+주소 중복 신청 막음. **2026-09-27부터 자동 연결·주소 좌표**(docs/21): 가입 때 상호·주소로 카카오를 다시 찾아 주소 같고 이름 겹치는 장소가 하나면 그 id로(`shared pickAutoLink`·`server/kakao autoLinkPlace`), 없으면 주소를 좌표로(`geocodeAddress`) 저장해 manual 매장도 검색·내 주변·상세·예약에 나온다(카카오맵 링크·구글 평점만 없음, id 검사는 `isPlaceId`). 승인 때 한 번 더 자동 연결. 그래도 안 되면 운영자가 `/admin/partners`의 **카카오맵 장소 연결**(카카오에서 다시 찾아 확인, `partners-admin.ts linkKakaoPlace`)을 누르면 id·이름·주소·좌표를 바꾸고 `place_info`도 새 id로 옮긴다(그 장소의 운영자 전용 값은 유지). **매장 메뉴판**(2026-09-19): 파트너 `/store`에서 메뉴판 사진 → Claude가 메뉴(음식명·설명·가격)·술(이름·용량·도수·가격)을 읽어 표에 더함(없는 칸은 빈칸, `shared/menu-items.ts`, `place_info.menu_items·drink_items` 0024, 읽기 본체 `packages/server/menu-read.ts`, 매장당 하루 20번) → 식당 카드·예약 화면 "메뉴판 보기"(`MenuBoard`: 한 줄 = 사진·이름·설명(술은 용량·도수)·가격, 카드 요약 줄에 사진 4장 `MenuThumbs`). **메뉴·술 사진**(2026-09-19): 표의 줄마다 [+ 사진] → 브라우저에서 긴 변 800px JPEG → 파트너 `/api/menu-photo` → Storage 공개 버킷 `menu-photos/{매장 id}/`(`packages/server/menu-photo.ts`, 1MB·매장당 400장) → 주소를 `menu_items`·`drink_items`의 `img`에. 주소는 우리 버킷 공개 주소만 통과(`shared cleanMenuImage`). 표에서 빠진 사진은 되돌리기 기간 때문에 바로 지우지 않고 아침 크론이 30일 뒤 삭제(`cleanupMenuPhotos`). 사진 권리는 파트너 책임(파트너 약관 4조). **식당 검색** `/places` + `/api/v1/places/search`(검색할 때만 카카오 호출, 주변 결과에 그 이름이 없으면 전국에서 찾아 앞에) + 술·음식 검색 결과 화면의 "‘검색어’ 식당" 칸(`SearchPlaces`, 이름·키워드 검색일 때만, `lite=1`로 구글 평점 생략·5곳). 식당 결과는 이름이 맞는 곳 먼저(`shared/places.ts sortByNameMatch` — 카카오는 메뉴명·태그까지 봐서 이름이 다른 곳도 준다). **검색창 자동완성**(`SearchBox`): 술·음식·종류/양조장은 `/api/v1/suggest`(shared search 그대로 — 부분 일치·입력 중 자모·초성, 외부 호출 없음, 0.12초), 식당은 두 글자 이상·0.4초 멈췄을 때만 `places/search?lite=1` → 이름 맞는 곳 3곳(`nameMatchedOrAll`), ↑↓ Enter(한글 조합 중 Enter 무시), 카드 목록은 `PlaceList`·붙이기는 `lib/place-enrich.ts`로 맛집 API와 공용. 레이트리밋은 `rateLimit(req, n, scope)` — 라우트마다 scope를 나눈다. **비밀번호 찾기**(2026-09-19): 휴대폰 문자 인증(`packages/server/password-reset.ts`, `password_resets` 0025) — 웹은 휴대폰을 인증한 이메일 회원만(`/forgot`), 파트너는 가입 때 담당자 번호(`/forgot`, 바꾸면 기존 로그인 끊김). 계정 존재 여부는 드러내지 않는다. **노쇼 제한**: 최근 90일 노쇼 2번 이상 → 마지막 노쇼로부터 30일 앱 예약 제한(`shared/reservation/noshow.ts`, 약관 제6조의2). **파트너 간편로그인**(카카오·네이버): Auth.js 없이 인가 코드 흐름 직접(`apps/partner/lib/oauth.ts`, state·가입 대기는 서명 쿠키), 리다이렉트 URI = 요청 주소 + `/api/oauth/{kakao|naver}/callback`(설정값 쓰지 않음), 연결 표 `partner_identities`(0027), 처음 오는 계정은 가입 신청으로(비밀번호 없이, `password_hash` "oauth:…"), 설정 화면에서 연결·끊기(비밀번호 없는 계정의 마지막 방법은 못 끊음), 프로필 정리 `shared/oauth-profile.ts`. **같은 사람 중복 가입 금지**(2026-09-19 사용자 결정, 두 앱): 이메일·카카오·네이버·구글 중 하나로 가입한 사람이 다른 방법으로 새로 가입하면 막고 "이미 가입된 계정 — ○○로 가입하셨어요"(`shared/account-dup.ts` `sameIdentity`·`duplicateMessage`, 로그인 화면 `?error=dup&via=`). 기준 = 같은 이메일(공급자가 인증한 것만) 또는 같은 휴대폰 번호(양쪽에 이름이 있으면 이름도 같아야) — 웹은 실명을 받지 않아 **문자 인증한 번호**만(`lib/account.ts existingAccountMethods`: 이메일 가입·`auth.ts` signIn 콜백·번호 인증 확정 때), 파트너는 가입 때 받은 이름 + 번호(`lib/partner.ts existingPartnerMethods`: 가입 신청·간편로그인 첫 방문). 자동으로 합치지 않는다(확인 안 된 이메일로 남의 계정에 붙는 사고 방지) — 파트너는 로그인 뒤 설정에서 연결. 동의 버전 2026-09-19.2. **운영 오류 기록**: `instrumentation.ts`(두 앱) `onRequestError` + 주요 실패 지점의 `reportError`(`packages/server/errors.ts`) → `server_errors`(0026, 같은 오류는 묶어 횟수, 번호·이메일 가림) → 어드민 `/admin/errors`·대시보드 알림. 화면 오류는 `error.tsx` → `/api/errors`. **로컬 개발 서버는 운영과 같은 DB라 기록하지 않는다** — Vercel(`VERCEL`) + production일 때만 기록(시험할 때만 `ERROR_LOG_DEV=1`). 동의 버전 2026-09-19(휴대폰 번호 이용 목적에 비밀번호 재설정 추가).
- **파트너 업종 3갈래**(2026-09-20 사용자 결정): `merchants.kind`(0029) = `restaurant` 식당 · `brewery` 양조장 · `liquor` 리쿼샵, 규칙 `shared/reservation/partner.ts`(`PARTNER_KINDS`·`PARTNER_KIND_LABEL`·`PARTNER_KIND_HINT`·`cleanPartnerKind`·`partnerTakesReservations`). 파트너 가입 첫 칸에서 고르고(기본 식당), 어드민 `/admin/partners`는 전체·식당·양조장·리쿼샵 탭(`?kind=`)과 이름 옆 업종 칩, 줄마다 **업종 바꾸기**(`action: "kind"`). **예약은 업종과 무관** — 예약 받기를 켠 승인 매장이면 받는다(식당 자리 예약 · 양조장 방문 시음 · 리쿼샵 방문 픽업, `PARTNER_RESERVATION_LABEL`). 예약 화면은 업종에 맞춰 제목·버튼 문구가 바뀌고, **콜키지 칸은 식당에만** 보인다(양조장·리쿼샵은 자기 술을 파는 곳 — `corkage="hide"`). 업종은 `Merchant.kind`로 서버에서 내려 준다(`packages/server/reservations` MERCHANT_COLS). **회차제**(0030, 2026-09-20): `reservation_settings.session_times`(HH:MM 최대 12개)·`session_minutes`(소요 시간 안내) — 비어 있으면 지금처럼 영업시간을 `slotMinutes`로 나눈다. 회차가 있으면 그 시각만(영업시간 밖·브레이크는 빠짐, `shared slotTimes(h, slotMinutes, sessionTimes)`·`cleanSessionTimes`), 정원은 회차당 팀·인원. **양조장 최소 인원은 2명부터**(`MIN_PARTY_BY_KIND`, `cleanSettings(raw, kind)` — 파트너 저장 때 업종으로 바닥값 적용). 파트너 `/settings`에 "받는 시간"·"한 번에 걸리는 시간" 칸, 손님 화면은 "회차 · 약 60분". **양조장 파트너 ↔ 카탈로그 양조장**(0031, `merchants.brewery`): 파트너 `/store`에서 "우리 양조장"을 카탈로그 이름으로 고르면(`saveBrewery`, 없는 이름은 거부) 매장 상세에 **그 양조장 전통주 목록**(`breweryDrinks`)이 뜨고, 술 상세에는 **"양조장 방문 시음 예약"** 상자(`breweryPartner`)가 붙는다. 식당 검색은 매장 이름·양조장 이름 + **그 양조장 술 이름**으로도 파트너를 찾는다(`partnerPlacesByName`). **파트너 매장은 카카오 분류(FD6)에 안 걸려도 이름으로 검색된다** — `/api/v1/places/search`가 `lib/place-info.ts partnerPlacesByName`(승인·좌표 있는 매장, 이름 부분일치)을 결과 맨 앞에 붙인다(2026-09-20 사용자 제보: 양조장이 검색에 안 나옴) — 이름 정렬(`sortByNameMatch`) 뒤에도 파트너를 맨 앞에 둔다(같은 이름 식당에 밀리지 않게). 블루리본 링크는 양조장·리쿼샵에 붙이지 않는다. **매장 화면은 업종에 맞게**(2026-09-21 사용자 요청): 파트너 `/store`에서 양조장·리쿼샵은 음식 메뉴 표를 숨기고 **술만**(제목 "우리 술"·"취급하는 술"), 술 줄에 **설명 칸**이 하나 더 생긴다(`DrinkItem.desc`, 손님 화면 메뉴판에도 설명이 보인다). 양조장은 `merchants.brewery`가 정해져 있으면 **우리 술 N종 불러오기**로 카탈로그 술을 표에 한 번에 담는다(이미 적은 이름은 건드리지 않음, `breweryDrinkOptions`). 사진 읽기 문구도 "술 목록 사진"으로 바뀌고 읽은 음식 줄은 버린다. **파트너 인증 도장**(2026-09-21 사용자 결정): 매장 상세·술 상세에 `✓ 파트너 식당/양조장/리쿼샵` 배지(`.seal`, site.css) — **술을 파는 곳(양조장·리쿼샵)은 로고 주황, 식당은 로고 남색**(`PARTNER_KIND_TONE`, 2026-09-24에 뒤집음 — 검색 결과 색과 잇는다). 공개 웹 site.css는 라이트 고정이라 이 배지에 다크 규칙을 두지 않는다. **파트너 화면 문구도 업종을 따른다**(2026-09-22 사용자 요청): "페어링GO 양조장 목록/카드"처럼 `PARTNER_PLACE_LABEL`로 부르고 한 줄 소개 예시는 `PARTNER_INTRO_EXAMPLE`(업종마다 다름). 메뉴·술 표는 줄마다 **↑ ↓ ×**(`.mops`)로 차례를 바꾸며, 저장한 차례 그대로 손님 메뉴판에 보인다(`shared/menu-items.ts moveItem`). 판매 탭은 `Tabs`에 `kind`를 넘겨야 보이므로 모든 파트너 화면이 넘긴다.
- **전통주 구매 — 중개(입점)**(2026-09-21 사용자 결정, docs/22): 직접 매입 판매은 준비·자금 부담으로 보류하고 **중개부터**. 판매자 = 입점 양조장(주류 통신판매 승인 보유, 전통주 제조자만 통신판매 가능), 배송·CS도 양조장, 우리는 **통신판매중개자 + 수단제공자** — 주류를 자기 책임으로 사거나 팔지 않는다(판매대행+정산 차감·계정 대리 운영은 동업경영으로 보아 양조장 면허가 위험, 국세청 서면-2023-소비-4309). 대금은 PG 하위몰로 양조장 앞 직접 정산, **수수료는 앱·PG를 나눠 적는다**(2026-09-21 사용자 결정, `shared/shop/fee.ts` `APP_FEE`·`PG_FEE`·`feeText`) — 앱 수수료 시범 3개월 0% → 5%, 결제수수료 2.5% 내외(PG 몫)라 파트너 부담 합계는 2.5% → 7.5%. 네이버 4.6~6.6%·쿠팡 식품 10.6%·옛 오픈마켓 전통주 12% 대비. 규칙 `shared/shop/`(배송비·택배사 `shipping.ts` — 기본 배송비 0=무료배송·무료 기준·도서산간·출고 소요일·냉장 가능, 택배사는 목록 12곳 또는 직접 입력(조회 링크는 목록만) / 상품 `product.ts` / 장바구니 `cart.ts` — **양조장별 묶음배송, 무료 기준은 그 양조장 금액으로만** / 주문 `order.ts` — `canOrderTransition`(예약의 canTransition과 이름이 다르다)·청약철회 7일·자동 구매확정 7일·우편번호 도서산간 판정). 표 0032(sellers·products·cart_items·orders·order_items·shipments·stock_logs·settlements) + DB 함수 `place_order()`(재고 잠금)·`cancel_order_items()`(재고 되돌림). 서버 `packages/server/shop.ts`·`shop-orders.ts`. 파트너 앱은 **양조장에만 판매 탭**(`/sell` 상품·재고, `/sell/setup` 입점·배송 설정, `/sell/orders` 발주 확인→발송(택배사·송장)→배송 완료). 웹은 술 상세 `BuyBox`(입점 상품이 있을 때만, 재고는 `/api/v1/shop/products`로 실시간)·`/cart`·`/checkout`·`/orders`, 어드민 `/admin/shop`(입점 승인은 **통신판매 승인 번호가 있어야** 가능·수수료율·지연 주문 경고). **결제(PG)는 계약 전이라 안 붙었다** — `SHOP_TEST_PAY=1`일 때만 시험 주문(pay_provider=test). **방침·약관은 2026-09-21에 개정**(동의 버전 `2026-09-21` — 주문 정보 수집·판매자 제3자 제공·전자상거래법 보관 5년/3년, 회원 약관 제6조의3 구매·청약철회, 파트너 약관 4의2 입점·5 수수료). PG·본인확인기관 이름은 계약 후 방침 위탁 표에 적고 공지만(재동의 없음). 판매를 열기 전 남은 것은 docs/22 §12-1(PG·성인인증·알림).
- **없는 술 요청**(2026-09-20, `/admin/wanted`): 결과가 없던 검색어(`search_logs` kind=search_empty) + 식당 메뉴판·확인 정보의 술 이름(`place_info.drink_names`·`drink_items`, 카탈로그에 없어 이름으로 남은 것) + 회원픽 `drink_raw`를 최근 90일치 모아 한 줄로 — 규칙 `shared/wanted.ts` `buildWantedList`(카탈로그에 있는 이름·음식·일반 낱말 제외, 무게 메뉴판 3 · 회원픽 2 · 검색 1), 서버 `lib/wanted.ts`. 줄마다 더술닷컴·요즘이술·네이버 검색 링크. 넣을 술은 `research/additions/` + `add-drinks`.
- 근거 검수 대기열(docs/20 P2-1): `/admin/review` 기본 화면은 근거 없는 술·음식(출처 official·sommelier·media·blog·user 페어링이 하나도 없는 것, 앱 카탈로그 기준)에 걸린 draft 후보만 — 같은 조합은 대표 1장, 둘 다 빈칸 → 한쪽 빈칸, 같은 술·음식이 몰리지 않게 감점(`shared/pairing/review-queue.ts` `buildReviewQueue`), 50장씩. 기존 목록은 `?view=all`.
- **전통주 수상**(2026-09-20 사용자 결정): 우리술품평회(농식품부·aT) + 대한민국주류대상(조선비즈) **우리술 부문만**, `/awards`는 대회 탭 × 연도 — 우리술품평회 5년 · **대한민국주류대상 2022년부터**(2026-09-24 사용자 결정, 전에는 3년 — `shared DRINK_COMPETITIONS.years`·`awardYearCount`, 명단은 이미 2022부터 있어 `drink-awards --apply`로 54종에 붙임). **상 받은 술 옆에는 받은 상 전부를 "연도 + 상"으로**(`shared awardLabels` — 최근 해부터, 같은 해는 높은 상부터, 대회 화면에서는 그 대회 것만 이름 생략·다른 대회는 "2025 품평회 대상"처럼 짧은 이름, 목록·검색은 모두 짧은 이름) — `/awards` 줄 아래 가로 전체(`.rows.awards .hist-line`), `/drinks` 카드, 검색 결과(`.award-hist`). 범위 밖 주류대상 수상은 붙이지 않고 이미 붙은 것도 뗀다(우리술품평회 옛 수상은 aT 자료라 그대로 둔다). drinks.awards 형식 통일 "2025 우리술품평회 과실주 대상"·"2026 대한민국주류대상 탁주 Best of Best"(대통령상·Best of Best가 맨 앞) — 규칙 `shared/drink-awards.ts`(`parseDrinkAward`·`drinkAwardString`·`mergeDrinkAwards`·`matchAwardDrink`). 명단 `packages/db/research/awards/`: `woorisool-fair.json`(보도자료, 해마다 손으로 추가) · `korea-liquor-awards.json`(`liquor-awards-fetch` — 요즘이술 yosool.co.kr 수상작 우리술 탭 + Best of Best 탭(여기 오른 술은 우리술 탭에서 빠짐), 새해는 `SEQ_AWARD`에 탭 번호, 소개글·심사평은 받지 않음) → `drink-awards`(미리보기 → `--apply` 발행 → `export`). 대조는 양조장 같음 + 이름 같음/도수·양조장 표기 차이만, 안 붙는 같은 술은 `match-overrides.json`(동명이주 주의: 조옥화 민속주 안동소주 ≠ 명인안동소주). 목록 확장 후보는 `expand-candidates` → `templates/전통주_추가후보.xlsx`(수상작 전부 + 수상 없는 후보 점수순 60, 수입 원료·대형 주류회사·무알코올 제외 — 온라인 판매는 전통주만), 판매처는 `buy-links`(업체가 요즘이술·더술닷컴에 등록한 링크만, 스마트스토어 페이지는 열지 않음, **modoo.at 주소는 쓰지 않는다**) → `add-candidates --apply`로 카탈로그에 넣는다(도수를 모르는 후보는 `research/candidate-abv.json`에 출처와 함께 적어 넣는다). 이미 있는 술의 빈 구매 링크는 `catalog-buy-links`(출처 ①더술닷컴 홈페이지 ②요즘이술 ③같은 양조장 ④`research/catalog-buy-manual.json` — 네이버 웹 검색 결과의 주소·제목만 보고 사람이 고른 공식몰·스마트스토어), **스마트스토어가 아닌 주소는 첫 화면을 열어 그 술 이름(별칭, 띄어쓰기 무시)이 보여야만 넣는다**(2026-09-24 사용자 규칙: 들어갔는데 다른 종류 제품만 나오면 없는 것으로 — `showsDrink`). **소매점 상품 페이지도 허용**(2026-09-24 사용자 결정 — 전통주 전문 소매점 키햐·술LOVE·술마켓·ZZANN·우체국쇼핑, `retail-links`가 키햐·술LOVE 검색 결과에서 제목에 술 낱말이 다 있고 술 낱말/양조장이 붙은 상품만 고름 — 오픈마켓(11번가·G마켓)·가격비교(다나와·에누리)·데일리샷은 안 씀, `buy.store`에 가게 이름). 2026-09-24 실측: 빈칸 71종 중 22종 채움(공식몰 9·스마트스토어 3·소매점 10), 49종은 오프라인·오픈마켓뿐이라 네이버쇼핑 검색으로 연결. 같은 술 중복은 `dup-check`.
- **공공데이터포털 전통주정보(aT) API**(2026-09-27, docs/11 §5-11): `packages/db datago`(받기 → `--match` 대조 → `--apply` 반영: 규격·성분·수상·홈페이지·안주 문장 후보) — 키·End Point는 `packages/db/.env`(`DATA_GO_KR_KEY`·`DATA_GO_KR_TRADLIQ_URL`), 이 데이터셋은 이용허락범위 제한 없음(출처 표시). 대조 예외는 `research/datago/overrides.json`. 안주 문장은 `pairing_candidates`(source_kind datago, official)로 들어가 검수 뒤 승격.
- 식당 수상 배지: 미쉐린만(`restaurant_awards`, 매년 docs/17 절차로 갱신, 로고 금지·자체 ★+연도). **블루리본 목록은 계약 전 절대 가져오지 않는다**(DB 제작자 권리·약관) — 검색 링크만.
- 전통주 지역별 목록(2026-09-11, docs/11 §5-1): `packages/db/research/`(더술닷컴 1,300종·찾아가는 양조장 64·네이버 백과) → `pnpm --filter @pairinggo/db regional` → `templates/전통주_지역별_목록.xlsx`. 시도 순서·정규화는 `packages/db/src/sido.ts` (광주는 전남에 묶음, 2026-07 통합). aT 문구는 공공누리 4유형 — 앱 화면에 원문 그대로 싣지 않는다.

## 구조 (pnpm workspaces)
- apps/miniapp      Vite + React 19 + TS + Tailwind v4 + react-router 7. @apps-in-toss/web-framework 3.x (apps-in-toss.config.ts, 테스트는 AIT Devtools 브라우저). **정적 번들만(SSR·서버 코드 금지)**. `pnpm --filter miniapp dev|build`
- packages/shared   @pairinggo/shared — 데이터(data/pairings.json)·타입·검색 엔진(search/)·페어링 점수(pairing/)·유사도·지역·별점. Vitest
- scripts/          check-links.mjs (주간 구매 링크 점검 → packages/shared/data/link-status.json)
- apps/web          Next.js 16 (Vercel) — **공개 반응형 웹 `(site)`**(`/`·`/drinks`·`/drinks/[slug]`·`/foods`·`/foods/[slug]`·sitemap·robots, 서버 렌더 = 검색 유입용) + 공개 API `/api/v1/*` + 크론 + **운영 어드민 `/admin`**(ADMIN_PASSWORD, 검수·승격·발행). SUPABASE_* 없으면 shared 내장 데이터로 정적 폴백(`x-pairinggo-source`). `pnpm dev:web`
  - 라우트 경로 세그먼트는 **영문만** — 한글 폴더명은 Next.js 정적 생성에서 깨진다. 슬러그(동적 구간)는 한글 유지(`/drinks/복순도가-손막걸리`), 대조는 shared `slugKey()`.
  - `body` 색은 구역 CSS(`site.css`·`admin.css`)가 정한다. 루트 레이아웃에 색을 고정하면 다크 모드가 깨진다.
- apps/partner      Next.js 16 — 페어링GO 파트너(식당 사장님) PWA. `pnpm --filter partner dev`(3001). 로그인·예약·매장 정보·예약 설정
- packages/server   @pairinggo/server — 두 앱 공용 서버 코드(db·password·kakao·reservations·merchant-store·sms·push·notify). **브라우저 번들 금지**, 웹의 lib/db·password·kakao는 다시 내보내기만
- packages/db       마이그레이션(SQL 0001~0006)·시드·export·**엑셀 가져오기(db:import)·자동 수집(db:collect)·status** — `postgres` 드라이버. DATABASE_URL은 packages/db/.env

## 절대 규칙
- 앱인토스 제약: 미니앱에 서버 코드·SSR 금지. 로그인은 토스 로그인만(Phase 3). 실물 결제는 토스페이만(Phase 5). 외부 결제창·외부 의존 링크 금지 — 예외는 법적 고지·제휴기관 공식 페이지·"제품 추천 후 구매 플랫폼 이동"(구매 버튼 문구: "양조장 공식몰로 이동").
- 외부 링크는 미니앱은 `apps/miniapp/src/lib/openExternal.ts`, 공개 웹은 `apps/web/app/(site)/_components/ExtLink.tsx`로만 연다(`<a target=_blank>` 직접 사용 금지 — 퍼널 이벤트가 빠진다). 웹 이벤트는 `apps/web/lib/track.ts`. 위치는 `lib/location.ts`(토스 SDK/브라우저)로만, 카카오 로컬 호출은 서버 라우트 `/api/v1/places/*`로만(클라이언트에서 dapi 직접 호출 금지, 쿼터·키 보호).
- **첫 화면은 무조건 로그아웃 상태**(사용자 결정) — 밖에서 들어오면(referrer가 우리 출처가 아니거나 새 탭) `SavedProvider`가 `/api/auth/reset`으로 세션을 지운다. 판정은 `packages/shared/src/session.ts`, 예외는 새로고침·로그인 직후(`AuthAttempt`)뿐. 자동 로그인 유지 기능을 넣지 않는다.
- 로그인은 **Auth.js v5** — 이메일(scrypt 해시, `lib/password.ts`) + 카카오·네이버·구글. Supabase Auth는 네이버 미지원이라 안 쓴다. 세션은 JWT, 우리 `users.id`를 토큰에 담는다.
- 세션·저장 상태는 **`SavedProvider` 한곳에서** 관리하고 경로가 바뀌면 다시 확인한다. 앱 라우터는 레이아웃을 리마운트하지 않아 로그인 직후 헤더가 안 바뀐다. 서버 컴포넌트에서 `auth()`를 부르면 공개 페이지 정적 생성이 깨지므로 공개 페이지에서는 쓰지 않는다.
- 저장(찜)은 전통주·음식·**음식점**. 음식점은 카탈로그에 없으므로 `saved_items.meta`(jsonb)에 이름·주소·링크를 함께 담는다. 카카오 로컬은 **사용자가 누를 때만** 부른다(유료 쿼터).
- 페어링 묶음: official·sommelier=**전문가픽**, media·blog=**대중픽**, profile·ai=맛 분석(`pairing/pick.ts`). 회원 '먹어봤어요'(어울렸다·보통·별로, `pairing_ratings`, `/api/ratings`)는 근거 점수·등급과 섞지 않는다. 첫 화면 로그아웃 판정은 **문서당 한 번**(앱 라우터 이동마다 다시 판정하면 링크로 들어와 로그인한 회원이 로그아웃된다).
- 회원 추천 페어링(회원픽, docs/13): 글은 바로 게시, 하트(`member_pick_likes`) 많은 순. 한 글 하트 `MEMBER_PICK_LIKES_MIN`(3) 또는 같은 조합 글 `MEMBER_PICK_MIN`(2)이면 `pairings` src 'user' 자동 생성(`lib/member-picks.ts`). users 조인은 FK 이름 지정(`users!member_picks_user_id_fkey`). 닉네임만 노출, 글 140자·링크 금지, 사진 1장(Storage `member-picks`), 카탈로그에 없는 이름은 `/admin/picks` 검수 후. 카탈로그 캐시는 15초마다 version을 확인한다(다른 프로세스의 발행 반영).
- 마이페이지 "먹어봤나요?"(`TriedCard`, 저장한 술·음식의 미평가 조합 3개, `shared/pairing/tried-suggest.ts`) · 추천인(`users.referred_by`, 0019, 가입·가입 마무리 때 닉네임으로, 보상은 "초대한 친구 N명"뿐) — docs/19 §3-1.
- 회원 프로필: 가입 때 닉네임(2~12자, 중복·링크·운영자 사칭 금지 `nicknameProblem`)·성별·생년월일(8자리 직접 입력 `birthDigitsToDate`, 만 19세 이상)·시도 필수(`packages/shared/src/profile.ts`), 소셜 회원은 `/profile`. 닉네임이 없거나 규칙 위반이면 `SavedProvider`가 `/profile`로 보낸다(`setupNeeded`). **간편로그인은 기존 회원의 닉네임을 덮어쓰지 않는다**(auth.ts upsertUser). 닉네임을 바꾸면 회원픽 근거(`pairing_evidence.who`)도 함께 바꾼다. 로그인 회원의 events·search_logs에 `user_id`가 붙는다.
- 개인정보처리방침 `/privacy`·이용약관 `/terms`(본문 `_components/legal/`, 운영자·시행일 `lib/legal.ts`) + 가입 동의(이용약관·개인정보 수집·이용·만 19세, `packages/shared/src/consent.ts`, `users.consent_version`). 간편가입은 계정 연결 뒤 `/profile`(가입 마무리)에서 동의 — `SavedProvider`가 동의 전 회원을 보낸다. **수집 항목·목적·위탁이 바뀌면 방침 본문 + `CONSENT_VERSION`을 올린다**(기존 회원 재동의). 탈퇴 `/withdraw`(`deleteAccount`: cascade + 근거 글·사진 삭제, 로그는 user_id null). 주문을 받으면 양조장에 주문자 정보를 넘기므로 제3자 제공 동의는 그때 별도.
- 비밀키는 apps/web/.env.local(SUPABASE_SERVICE_ROLE_KEY·CRON_SECRET·AUTH_*)과 packages/db/.env(DATABASE_URL)에만. 미니앱 번들엔 VITE_API_BASE_URL·VITE_KAKAO_JS_KEY(도메인 제한 공개 키)만. KAKAO_REST_KEY는 apps/web 서버 전용. 채팅·커밋에 키 금지.
- 카탈로그 파생값(D·F·byDrink·DOCS 등)은 `export let` 라이브 바인딩 — 모듈 로드 시 복사하지 말고 사용 시점에 읽는다(applyDataset 핫스왑 대응).
- 온라인 판매 불가 주류(`NON_TRAD`, online_sellable=false)는 구매 링크 대신 "온라인 직배송 불가" 안내.
- 주류 경고문구·만 19세 안내는 전 페이지 공통 푸터. AI 생성 페어링은 `source: ai` 배지(Phase 9).
- 모든 UI 텍스트 한국어. **미니앱은 모바일 고정 390px(최대 430px 중앙) — 데스크톱까지 넓히지 않는다**(그대로 앱이 된다). **공개 웹(`apps/web/(site)`)은 반응형**. 라이트·다크 모두 토큰으로.
- 외부 링크는 실제로 열렸을 때만 퍼널 이벤트를 남긴다(실패는 `link_open_failed`). `buy_link_click`은 입점 제안 자료이자 `refresh_pairing_feedback` 입력이라 부풀리면 안 된다.
- 페어링 순위 = 등급 → 근거 링크 있음(+3) → 점수(`pairing/score.ts`). 맛 분석 점수(pf.s)는 **근거 조합에서 배운 친화도**(`pairing/affinity.ts`: 술 종류 × 음식(분류에서 출발해 그 음식 자체의 근거로 보정)·매운 음식·기름진 음식의 lift 로그 합 → 50이 중립인 0~100 곡선 `affinityScore`, 2026-09-17) — 예전 `profileFit` 규칙은 근거 조합을 무작위 수준(AUC 0.50)으로밖에 못 골라 점수에서 빼고 카드 문구로만 쓴다. 점수 규칙을 바꿀 때는 `pairing/fit-eval.ts`의 교차검증 AUC로 재고(테스트가 0.56 미만이면 실패), 근거가 늘면 `pf-recalc`를 다시 돌린다. (이전 설명: 카탈로그 전체 `profileFit` 백분위 한 눈금 — 술·페어링을 넣거나 맛 규칙을 바꾸면 `pnpm --filter @pairinggo/db pf-recalc` → `db:export`(docs/18 §1-1).
- 검색·점수·유사도 로직은 packages/shared에만 두고 테스트를 먼저 쓴다. 컴포넌트에 계산 로직을 넣지 않는다.
- 커밋은 기능 단위로 작게. 커밋 메시지는 한국어 요약 + 영어 scope 허용.

## 데이터
- 원본: packages/shared/data/pairings.json (전통주 557 · 음식 143 · 페어링 4,840 — **2026-09-24 주류대상 2022~2023 수상작 +39종**(`add-candidates --awards-only`, 이름 고치기 `research/candidate-names.json`, 대형 주류회사 제외에 골든블루 추가, 같은 술 표기 차이는 `match-overrides.json` 13건 — 이름이 겹쳐 못 넣은 것: 리밋브루잉 ‘달’(별빛드리운못 ‘달’과 같은 이름), 국순당·하이트진로는 대형 주류회사라 제외), 2026-09-20 목록 확장 +293종(284 + 도수 확인 9)(수상작·더술닷컴 후보, `add-candidates`) docs/11 §5-8, 이전 2,184 — 2026-09-13 라인업 확장 +99종 §5-4, 백경증류소 +7종·한증류소 +3종 §5-5, 2026-09-14 음식 확장 +33종 양식·중식·일식·아시아 §5-6, 2026-09-15 다농바이오 +3·신선주 +5 §5-7, docs/11). 새 음식은 `add-foods`(맛 분석 `planDrinksForFood`). DB가 원본이고 JSON은 `db:export`로 내보낸 번들.
- 라인업 확장(`shop-insight`·`lineup`): 네이버 쇼핑 검색 API는 2026-07-31 종료, 스마트스토어 자동 수집은 약관 위반 → 쇼핑인사이트만. 새 술 설명은 사실로 새로 짓고(aT 원문 금지) 맛 프로필·맛 분석 페어링은 추정값. drinks.alias[0]은 짧은 이름(양조장 이름 넣지 않기). 필드: drinks{id,name,alias,category,abv,region,brewery,desc,flavor[],profile{sweet,acid,body,fizz,aroma},awards[],buy{url,store},offline{},trend{},blog_anju,generic} · foods{id,name,category,tags[],profile{fat,spice,umami,salt,sweet,weight},alias[],trend{},new} · pairings{d,f,es,reason,blog,src,ev{source,url,quote,who},pf{s,plus[],minus[]}}
- 대중 언급 수(`blog_count`)는 네이버 블로그 결과 수 — **이름만 검색해 100만 건이 넘는 흔한 이름은 쓰지 않고(0), 조합 수는 이름 단독 수로 자른다**(`shared/pairing/blog-count.ts` `blogNameUsable`·`capByNameTotal`, 2026-09-20). 새 술을 넣은 뒤에는 `blog-counts --all`.
- 출처 등급 src: official(양조장 공식) > sommelier(소믈리에·명인) > media(전문 매체) > profile(맛 프로필)
- 온라인 판매 불가 7종: d12 d13 d21 d32 d43 d52 d55

## 작업 방식
- 각 Phase는 플랜 모드로 시작해 계획 확인 후 실행. 한 대화 = 한 Phase.
- Phase 완료 시 docs/05의 검증 체크리스트를 통과했는지 확인하고 결과를 표로 보고.
- 실기기 확인: `pnpm --filter miniapp dev`(vite --host) → 샌드박스 앱에서 `intoss://pairinggo`
- 용어: 종합 점수 overall / 전문가 es / 대중 blog / 맛 프로필 pf.s
