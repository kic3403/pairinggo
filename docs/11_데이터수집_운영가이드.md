# 11. 페어링 데이터 수집·검수·발행 운영 가이드 (Phase 2.5)

작성 2026-09-10 · 도구 3종: 엑셀 가져오기 · 자동 수집 · 검수 화면. 흐름은 항상 **후보 큐 → 검수 → 승격 → 발행 → 앱 핫스왑**.

## 0. 원칙

- 페어링은 **근거가 붙은 주장**이다. 근거 없는 조합은 `pairing_candidates`에 머물고, 승인돼야 `pairings`+`pairing_evidence`가 된다.
- **승격 규칙**: `official`/`sommelier` 근거 1개 또는 근거 2개 이상 → 게시(`curated`). 그 외 → 보류(`pending`, 앱에 안 나감). 두 번째 근거가 승인되면 자동으로 `curated`.
- 출처 등급과 점수대: official 95~97 · sommelier 92~94 · media 88~91 · **blog 84~87** · user 84~87. 점수를 비우면 등급 기본값(96·93·89·85·85).
- 인용문은 **120자 이내**, 출처 URL 필수, 원문 통째 복제 금지(저작권). 같은 URL의 근거는 한 페어링에 한 번만(자동 차단).
- 인스타그램은 약관 때문에 수집하지 않는다. 네이버·유튜브는 공식 API만.
- 우선순위: ① 입점 예정 양조장 제품 ② 페어링 5개 미만인 술(대시보드) ③ "찾았는데 없던 검색어"(대시보드) ④ 인기 검색어.

## 1. 엑셀로 직접 넣기 (양조장 홈페이지·식당·소믈리에 지인)

1. `pnpm db:template` → `packages/db/templates/pairing-candidates.xlsx` (이미 생성돼 있음)
2. **후보** 시트에 한 줄 = 술 × 음식 × 근거 하나. 열: 술이름* · 음식이름* · 추천이유 · 출처명 · 출처URL · 인용문 · 추천자 · 출처등급(드롭다운) · 점수 · 메모
   - 술·음식 이름은 **술 목록 / 음식 목록** 시트의 이름(별칭 가능) 그대로 → 자동 매칭. 목록에 없으면 그대로 적으면 "확인 필요"로 들어가 검수 화면에서 지정
   - 소믈리에 지인 추천은 출처등급 `sommelier`, 추천자에 "이름 (직함)", 출처명에 "○○ 인터뷰/시음" 처럼
   - 양조장이 알려준 건 `official`, 추천자에 양조장명
3. `pnpm db:import 파일경로.xlsx` (CSV도 됨) → `성공 N · 확인 필요 N · 중복 N`, 확인 필요 목록은 `packages/db/import-report.md`
4. 검수 화면에서 승인

**양조장에 물어보는 폼 (권장)**: 입점 제안 메일에 "귀사 술에 가장 어울리는 안주 3가지와 한 줄 이유"를 붙이고 답을 그대로 엑셀 `official`로 넣는다.

## 2. 자동 수집 (네이버 블로그·카페·뉴스 + 유튜브)

```bash
pnpm db:collect --top 10                 # 인기 술 10종, 4개 소스
pnpm db:collect --drink d01,d38 --naver-news --youtube
pnpm db:collect --all --dry              # DB에 안 넣고 요약만
```
- 검색어: `"{술 별칭}" 안주` / `페어링` / `어울리는 음식` (따옴표 = 정확 구문, "서울의밤" 같은 일반 단어 대응)
- 추출: 제목+요약에서 **카탈로그 음식 이름·별칭이 등장**하면 후보 1건(AI 없음). 뉴스는 `media`, 나머지는 `blog`. 같은 (술, 음식, URL)은 중복 차단
- 결과 `packages/db/collect-report.md`. 후보에는 검색어·언급 수가 붙어 검수 화면에서 "언급 N" 배지로 보인다
- 한계: 이름만 스치는 글(주점 메뉴판, 산행기)도 잡힌다 → 검수에서 "관련 없음"으로 거절. 파일럿(금정산성·복순도가 44건 → 후보 31건)에서 절반 정도가 유효했다

### 키 발급 (각 5~10분)
- **네이버 (2026-09 기준, NAVER API HUB)**: 개발자센터(developers.naver.com)의 검색 API 신규 신청은 2026-07 중단됐고 네이버클라우드 플랫폼의 **NAVER API HUB**로 이관됐다(기존 키는 2027-06-30까지만).
  1. https://www.ncloud.com 회원가입(개인 가능, 본인인증 + **결제수단(카드) 등록 필수** — API HUB 자체는 현재 무료, 유료 전환 시 사전 공지)
  2. 콘솔 → Menu → All Services → Application Services → **NAVER API HUB** → Subscription **[서비스 이용 신청]** → 약관 동의
  3. **Application** → **[Application 등록]** → 이름 `pairinggo`, API에서 **검색(블로그·카페글·뉴스)** 선택 → 등록
  4. Application 목록 → **[인증 정보]** → Client ID → `NCP_API_KEY_ID=`, Client Secret → `NCP_API_KEY=` (`packages/db/.env`)
  - 엔드포인트 `https://naverapihub.apigw.ntruss.com/search/v1/{blog|cafearticle|news}`, 헤더 `X-NCP-APIGW-API-KEY-ID` / `X-NCP-APIGW-API-KEY` — collect.ts가 새 키가 있으면 자동으로 HUB 방식을 쓴다. 무료 한도 검색 월 775,000건·키당 50 RPS(초과 시 429)
- **유튜브**: https://console.cloud.google.com → 프로젝트 생성 → [API 및 서비스] → [라이브러리] → **YouTube Data API v3** 사용 설정 → [사용자 인증 정보] → **API 키** 만들기 → `YOUTUBE_API_KEY=`. 일 10,000 유닛(검색 1회 = 100유닛 → 하루 100회)

## 3. 검수 화면 (`/admin`)

- 접속: `http://localhost:3000/admin` (배포 후엔 서버 주소/admin). 비밀번호는 `apps/web/.env.local`의 `ADMIN_PASSWORD` — **기본값을 바꿔서 쓰세요**. 폰에서도 됨
- **대시보드**: 대기·지정 필요·승격 수, 페어링 5개 미만인 술, 없던 검색어(수집 우선순위), 미발행 승격 수
- **검수**: 언급 많은 순 카드. 필터 상태·등급·술. 카드에 술×음식, 인용문·원문 링크·검색어, 기존 페어링 유무(있으면 근거만 추가; 같은 URL이면 "거절 권장")
  - **승인**: 등급·점수·추천자 확인, **추천 이유 필수**(앱 카드에 그대로 표시) → 승격 결과 토스트(게시/보류)
  - **거절**: 사유 선택(관련 없음·광고·근거 부족·중복·매칭 오류)
  - **술·음식 지정**: 매칭 안 된 후보(needs_entity)는 검색으로 골라 저장 → 대기로
  - 단축키 A 승인 · R 거절 · S 건너뛰기 · ↑↓
- **발행**: 미발행 승격 수 확인 → 발행 → `catalog_meta.version` 갱신 + 스냅샷 저장. 미니앱은 다음 시작 때 자동 갱신(재검수 불필요). 보류(pending)는 포함 안 됨
- 검수자 이름은 브라우저에 기억되며 근거의 추천자·후보의 reviewer로 남는다

## 4. 소믈리에 지인에게 부탁할 때

1. 검수 화면 주소와 비밀번호 전달 (전용 비밀번호를 쓰고 싶으면 Phase 4에서 계정 분리)
2. "카드마다 10초: 맞으면 A, 아니면 R. 이유는 한 줄이면 됩니다" — 승인 시 추천자 칸에 본인 이름·직함을 적으면 `sommelier` 근거가 된다
3. 한 시간에 200~300건 가능. 끝나면 운영자가 발행

## 5. 파일럿 결과 (2026-09-10, 키 없이 MCP 도구로)

| 술 | 검색 결과 | 후보 | 음식 종류 | 상위 언급 |
|---|---|---|---|---|
| 금정산성막걸리 | 27 | 15 | 7 | 해물파전 6 · 오리구이 3 · 백숙 2 · 비빔국수·보쌈·회무침·족발 1 |
| 복순도가 손막걸리 | 17 | 16 | 13 | 감자전 2 · 치즈플래터 2 · 홍어삼합·방어회·두부김치·김치전·짜장면·후라이드치킨·떡갈비 … 1 |

검수 테스트: 영남일보(파전)·월간산(회무침)은 시드 근거와 같은 URL → 중복 차단 확인 · 복순도가×감자전 블로그 2건 → pending → curated → 발행(852건). "서울의밤"은 따옴표 없이 검색하면 노이즈만 — 따옴표 검색으로 수정.

## 5-1. 전통주 지역별 목록 (2026-09-11 추가)

`packages/db/templates/전통주_지역별_목록.xlsx` (`pnpm --filter @pairinggo/db regional`로 재생성). 후보 템플릿의 **술 목록** 시트도 같은 시도 순서로 묶임.

- **출처**: ① 더술닷컴(농식품부·aT 운영 공식 DB) 우리술 찾기 **1,300종 상세 전수** — 종류·원재료·도수·용량·수상내역·제품소개·**어울리는 음식(양조장 등록)**·양조장 주소 ② 공공데이터포털 15048756 찾아가는 양조장 64개소 ③ 우리술 품평회 2020~2025 수상작 96건(농식품부 보도자료·더술닷컴 카탈로그) ④ 네이버 전통주백과 「지역별 술」 94종(더술닷컴에 없는 32종만 추가) ⑤ 앱 카탈로그(108종, 88종은 더술닷컴과 매칭·19종은 카탈로그에만)
- **시트**: 요약 / 전체 목록 / 시도별 16개(서울 → 경기 → 인천 → 충남 → 세종 → 충북 → 대전 → 강원 → 전북 → 전남·광주 → 경북 → 대구 → 경남 → 울산 → 부산 → 제주, 광주는 2026-07-01 전남광주통합특별시로 통합) / 우리술품평회 2020~2025 / 찾아가는 양조장 64 / 출처·기준
- **쓰는 법**: "어울리는 음식(aT)" 열은 양조장이 직접 등록한 공식 추천 → 후보 템플릿에 `official` 등급으로 옮기면 바로 승격 가능. "앱 카탈로그" 열이 빈 술은 아직 앱에 없는 술 = 입점·확장 후보. aT 문구는 공공누리 4유형(변경금지)이라 앱 화면에는 요약해서 재작성.
- 원본 데이터는 `packages/db/research/` (thesool-products.json · visiting-brewery.csv · naver-regional.json · **manual-additions.json** = 더술닷컴에 없는 제품을 웹 조사로 보탠 것). 시도 정규화는 `src/sido.ts`, 로더·양조장 개명 표(`두레박 → 한증류소`, 브랜드 '나날', 공식 스토어 smartstore.naver.com/nanal1992)는 `src/research.ts`.
- 후보 템플릿의 **술 목록** 시트는 앱 등록 108종 + 같은 양조장의 다른 제품(양조장당 최대 3종, '앱 등록' × 표시) + 추가 양조장(세종 사일로·백경증류소)으로 확장됨. 제품을 더 넣으려면 `manual-additions.json`에 한 줄 추가 후 `pnpm db:template`.

## 5-2. "많이 찾는 전통주" 일일 집계 (2026-09-12 추가)

홈 배너·"많이 찾는 전통주"·미니앱 인기 순위의 기준. **채널 4개의 최근 30일 언급량을 채널별 0~100으로 맞춘 뒤 평균**한다. 매일 00:00(KST) Vercel 크론 `/api/cron/mentions`가 돌린다(`vercel.json` `0 15 * * *` UTC).

| 채널 | 세는 것 | 방법 | 한도·주기 |
|---|---|---|---|
| 네이버 블로그 | 최근 30일 글 수 | NAVER API HUB 블로그 검색 `sort=date`로 넘기며 `postdate` 30일 안만 셈. 제목+요약에 술 이름이 있고 술 맥락 단어가 있는 글만 | 상한 1,000(API 페이지 한계). 매일 전체 |
| 유튜브 | 최근 30일 영상 수 | Data API `search.list` `publishedAfter`. 제목+설명 언급 판정 | 상한 100. 검색 1회=100유닛·일 10,000 → **하루 절반씩**(짝수일 앞 절반·홀수일 뒤 절반, 7일 넘게 빈 술은 보충) |
| 구글 블로그 | 최근 30일 페이지 수(추정치) | Programmable Search JSON API `dateRestrict=m1`의 `totalResults`. 검색엔진(cx)은 블로그 도메인(tistory.com·brunch.co.kr·velog.io·blogspot.com·medium.com)으로 제한 | 무료 100회/일 → 하루 절반씩. 키 없으면 채널 제외 |
| 인스타그램 | 최근 30일 해시태그 게시물 수 | **자동 불가** — 그래프 API는 비즈니스 계정+앱 심사가 필요하고 게시물 수를 주지 않으며, 크롤링은 약관 위반. 앱에서 직접 세어 CSV로 넣는다: `pnpm --filter @pairinggo/db mentions:insta 파일.csv` (열: 술이름,게시물수) | 7일 안의 값만 쓴다 → 주 1회 입력 |

- 점수: `packages/shared/src/trend.ts` `scoreMentions()`. 채널별 최다 언급 술 = 100, 나머지 비례. 술 점수 = **값이 있는 채널의 평균**(언급 0건은 0점, 아예 못 센 채널은 제외). 예) 인스타 100·네이버 50·구글 50·유튜브 50 → (100+50+50+50)/4 = **62.5**. 동점이면 언급 총량이 많은 쪽이 위.
- 저장: `drink_mentions_daily`(0010, 날짜×술×채널 한 행) → `drinks.trend`(naver/insta/youtube/google 점수·score·rank·raw 원값) → `catalog_meta.trend_meta`(기간·채널 설명 문구, 화면 "기준" 표시) → 상위 20 순서가 바뀐 날만 카탈로그 발행(미니앱도 받는다).
- 검색어: 별칭(`alias`). 일반 단어와 겹치는 술은 `apps/web/lib/mentions.ts` `QUERY_OVERRIDE`(화요→"화요 소주", 예담→"국순당 예담"). `generic` 술(겨울소주 45·화주·허니문·감싸주는 날)은 세지 않는다. 응답 `log`의 "상한 도달" 술은 검색어가 겹칠 가능성이 크니 확인.
- 수동 실행: `GET /api/cron/mentions?all=1`(회전 무시 전부) · `?channel=naver,youtube` · `?dry=1`(저장 안 함). 헤더 `Authorization: Bearer <CRON_SECRET>`. 셸에서는 curl 대신 Node fetch.
- 키: Vercel 환경변수 `NCP_API_KEY_ID`·`NCP_API_KEY`·`YOUTUBE_API_KEY`(packages/db/.env와 같은 값)·`GOOGLE_CSE_KEY`·`GOOGLE_CSE_CX`(console.cloud.google.com → Custom Search API 사용 설정 → API 키 / programmablesearchengine.google.com → 검색엔진 만들기 → 검색할 사이트에 블로그 도메인 5개 → 검색엔진 ID).
- 첫 실행 실측(2026-09-12, 네이버 전체 104종 + 유튜브 20종): 네이버 최다 고창 복분자주 258건, 유튜브 최다 지평생막걸리 47편. 유튜브는 검색 20회 뒤 429(할당량)로 멈춤 → 다음 날 자동 보충. 소요 약 1분(할당량 오류를 재시도하지 않게 고친 뒤).

## 5-3. 상위 30종 근거 집중 검수 (2026-09-12)

"근거와 함께"라는 약속을 인기 술부터 지키기 위해, 인기 순위 상위 30종의 후보 큐(draft 4,219건)를 한 번에 검수했다.

- **사전 필터**: 인용문에 술 이름(별칭)과 음식 이름(별칭)이 모두 있고 페어링 단서 단어(어울·곁들·안주·페어링·함께·궁합·추천…)가 있는 후보만 → 1,351건. "제철 과일"은 딸기·배 같은 원료 단어에 걸리는 오탐이라 제외. 조합별로 URL 중복을 빼고 뉴스 > 카페 > 블로그 순으로 최대 3건 → 607건을 사람이 읽었다.
- **판정 기준**: 그 술과 그 음식을 같이 먹었다/어울린다/추천한다는 문장만 인정. 메뉴판 나열, 다른 술의 추천(예: 국순당 '예담' 설명에 백세주가 스쳐 지나가는 글, 최자 '분자'는 고창 복분자주가 아님, 한영석 청명주는 중원당 '도한' 청명주가 아님), 부정 표현("만두랑 안 어울릴 것 같아 패스"), 행사 안내는 제외. 같은 브랜드의 다른 도수(화요 19·53, 해창 9·18도, 오미로제 연)는 인정하되 점수는 낮게.
- **점수·등급**: 블로그·카페 84~86, 매체 85~89, 양조장·명인·셰프 발언은 official/sommelier 88~93(배상면주가 관계자, 명인안동소주 김 이사, 감홍로 이기숙 명인, 한강주조 대표, 김호윤 셰프, 밍글스, 대동여주도 이지민 대표, 전통주갤러리).
- **결과**: 262건 승격 → 새 페어링 117건, 근거 2개 이상(curated) 126조합, 근거 1개(pending, 화면 비공개) 51조합. 상위 30종 페어링 397건 중 근거 있는 것 221건(검수 전 79건), 근거 2개 이상 102건(검수 전 1건). 전체 카탈로그는 페어링 918건(공개 기준, 검수 전 851), 근거 있는 페어링 429건(검수 전 약 280). 발행 버전 2026-09-12T07:17:15Z.
- **2026-09-13 보완**: 승격 때 blog_count를 0으로 넣던 문제로 새 페어링 117건의 대중 언급 점수가 비어 있었다. 146건(공개 95·검수 중 51)을 네이버 검색 결과 수로 채웠고(136건 변경, 10건은 실제 0건), 이제 승격 시 자동으로 센다(`apps/web/lib/blog-count.ts`). 알려진 한계: 별칭이 일반 단어와 겹치는 술(화요→화요일)은 기존 데이터부터 부풀려져 있다 — 기존 값과 기준을 맞추려고 같은 규칙을 썼다.
- **남은 것**: 공주 밤막걸리·오미자생술·추사50·성수주조장 딸기막걸리는 쓸 만한 후보가 거의 없었다(수집 검색어 문제 — 별칭 "밤막걸리"는 일반어, 딸기막걸리는 다른 제품이 섞임). 다음 수집은 `--drink`로 이 넷만 검색어를 바꿔 돌린다. pending 51조합은 근거 1개를 더 찾으면 자동 공개된다.
- 검수 기록: 후보의 `reviewer` = "Claude 검수(상위30 근거)". 잘못 승격된 것을 발견하면 어드민에서 해당 근거를 지우고 페어링을 hidden으로.

## 5-4. 전통주 라인업 확장 — 네이버 쇼핑 수요 기준 (2026-09-13)

108종 → **207종**(+99), 페어링 918 → **1,710**(+792). 사용자 요청 "네이버 스마트스토어를 분석해 라인업을 늘려 달라".

- **스마트스토어 상품 목록을 직접 쓰지 못한 이유**: 네이버 쇼핑 검색 API(`/v1/search/shop.json`)는 **2026-07-31 종료**(NAVER API HUB 이관 대상 아님, 실측 404), 스마트스토어 페이지 자동 수집은 네이버 약관 위반. 대신 **쇼핑인사이트**(API HUB `/shopping/v1/category/keywords`)로 네이버 쇼핑 전체(스마트스토어 포함)의 키워드 클릭 추이를 쟀다.
- **후보**: `research/`의 더술닷컴 1,300종 + 수동 추가 중 앱에 없는 제품 → 이름 묶음 1,072개(같은 제품의 용량·도수 표기는 한 묶음).
- **수요 측정** (`pnpm --filter @pairinggo/db shop-insight`): 식품 카테고리(50000006), 2025-09~2026-08 월별. 요청마다 기준 키워드 '도깨비술'을 함께 넣어 기준=100 눈금으로 환산(쇼핑인사이트는 요청 안 최댓값=100 상대값만 준다). **키워드당 검색어 1개만 받는다**(여러 개면 400). 흔한 이름(생막걸리·사과와인·삼해주·포천막걸리 — 흔한 말·원료·지명·술 종류 이름만 남는 이름)은 양조장 이름을 붙여 검색. 현재 라인업 108종도 같은 눈금으로 재서 비교: 상위 25% 422 · 중앙값 52 · 하위 25% 3.
- **교차 확인** (`shop-insight --blog`): 쇼핑 클릭이 과일(홍시 4,082)·다른 상품(게이샤)·관용구(일엽편주)의 몫인지 네이버 블로그 50건으로 확인 — 이름이 들어간 글 중 이름을 지우고도 술 맥락 단어(술·막걸리·양조·안주·시음…)가 남는 비율.
- **선정 기준**(`LINEUP_RULE`, `packages/shared/src/lineup/lineup.ts`): 수요 ≥ 20(현재 라인업 하위 40% 수준) · 12개월 중 6개월 이상 클릭 · 블로그 글 5건 이상 중 술 이야기 60% 이상(이름에 술 단어가 있으면 45%) · 현재 라인업 제품의 다른 표기 제외 · 같은 이름을 여러 양조장이 쓰면 제외 · 양조장 이름과 같은 제품명 제외 · 양조장당 3종 · 먼저 뽑힌 제품의 다른 표기 제외.
- **결과**: 99종(탁주 33 · 증류주 22 · 약주 17 · 과실주 16 · 리큐르 9 · 허니와인 2). 수요 상위: 경탁주 12도 · 독도소주 · 빙탄복 · 마루나약주 · 사일로 막걸리 · 김포예주 · 오매락 · 대통주 · 신례명주 · 고운달 오크. 제외 973: 수요 낮음 411 · 클릭 없음 395 · 블로그 확인 안 됨 96 · 라인업 변형 31 · 이름 중복 16 · 양조장 이름 15 · 기타 9. 전체 표: `templates/전통주_라인업_확장.xlsx`(요약·선정·후보 전체·현재 라인업 수요), 선정 데이터 `research/lineup-plan.json`, 측정 원자료 `research/shop-insight.json`.
- **새 술 항목**: 이름·양조장·도수·지역·원료·수상(우리술품평회만)은 공개 제품 정보의 사실. **설명 문장은 사실로 새로 지었다**("경기 포천 배상면주가에서 복분자로 빚은 7도 과실주. 탄산·복분자향·숙성향이 특징입니다.") — 더술닷컴 소개글은 공공누리 4유형(변경 금지)이라 옮기지 않는다. 맛 프로필(단·산·바디·탄산·향)은 종류별 평균에서 출발해 원료·소개의 단어로 가감한 **추정값**. 구매 링크는 비워 두어 네이버쇼핑 검색으로 연결(스마트스토어 판매처가 여기서 나온다). 사진 없음.
- **새 페어링**(술당 8개): ① 양조장이 더술닷컴 제품 정보에 등록한 추천 음식 → `official` es 90, 근거 링크(95건, 54종) — 인용문 검수를 거친 공식 추천(91~97)보다 한 단계 아래라 대부분 '잘 어울림' ② 나머지는 **맛 분석**(`profile` es 84~87, 화면 '맛 분석' 탭, 697건, 대부분 '시도해 볼 만').
  - 맛 분석 순서는 **같은 종류 전통주에서 근거 있는 조합에 자주 나온 음식**이 먼저, 맛 프로필 궁합은 감점 조합을 빼고 동점을 가르는 데만. 근거: 기존 81종을 하나씩 빼고 근거 조합을 맞혀 본 실측 — 맛 프로필 궁합만으로 고른 8개는 **8.5%**(무작위 7.3%), 같은 종류 친화도는 **22.3%**. 앞 3개는 점수 순, 나머지는 상위 20개 후보 중 이번 확장에 덜 쓴 음식부터(같은 종류 술이 모두 같은 8개가 되지 않게 — 음식 102종에 퍼짐).
  - 맛 프로필 궁합 규칙(`profileFit`)은 기존 918조합의 이유 문구 16종과 조건을 데이터에서 역추적했다(기존 이유와 60% 이상 일치 테스트).
  - 대중 언급 수는 `blog-counts`로 채움(중앙값 29). **alias[0]은 짧은 이름으로 쓰인다**(화면·검색·언급 수집) — 처음에 양조장 이름("우리술")이 들어가 "우리술 두부김치" 40만 건이 잡혀 바로잡았다.
- **한계·다음**: 맛 프로필·맛 분석은 추정 — 양조장 확인·시음·회원 '먹어봤어요'로 고쳐 나간다. 새 술 99종은 근거 수집(`db:collect --drink`) 대상. 치즈플래터처럼 여러 종류에 두루 나오는 음식은 목록이 길어진다(치즈플래터 94종). 스크립트를 다시 돌리면 이번에 넣은 99종은 '현재 라인업'이 되어 후보에서 빠진다.

## 5-5. 양조장 라인 직접 추가 — 백경증류소 (2026-09-13)

사용자 요청: "세종 백경증류소 약주·증류주 라인 추가". 쇼핑 수요 기준(§5-4)에서는 빠졌다 — 제품명이 백경15 화이트·백경53처럼 도수·색으로 갈려 검색어가 쪼개지고, 블로그 교차 확인에서 이름이 든 글이 5건 미만이었다. 사람이 확인한 사실로 넣는 경로를 따로 만들었다.

- **경로**: `research/additions/<양조장>.json`(제품 사실·설명·맛 프로필·근거 조합) → `pnpm --filter @pairinggo/db add-drinks <json>`(미리보기) → `--apply`(DB 추가 + 발행) → `blog-counts` → `db:export`. 저장·발행은 라인업 확장과 같은 `src/catalog-write.ts`.
- **추가 7종**(d208~d214): 약주 백경15 화이트·골드·블랙·향온(모두 15%), 증류주 세종소주28·세종소주46·백경53. 확인 출처: 술달다 제품 목록(도수·용량·원료·가격), 더술닷컴 백경15 화이트(양조장 등록), 공식 사이트(스마트스토어 판매), 국가대표 전통주 소믈리에 블로그(화이트·골드·블랙은 누룩 차이, 화이트<골드<블랙 순으로 무거운 음식), 방문기(화이트 8·골드 5브릭스), 세종소주28·46·백경53 시음기. 목록은 JSON의 `sources`.
- **구매·방문**: 구매 버튼 = 백경증류소 공식 스마트스토어(공식 사이트가 안내하는 판매처). 양조장 주소·전화·방문 시음 안내(`offline`).
- **근거 조합 14건**: 화이트 — 족발·불고기·보쌈(수육)·피자는 양조장 등록 추천(official 90), 샤브샤브·월남쌈·육회는 시음 후기(blog 85), 족발·불고기는 후기 근거도 함께. 골드 — 갈비찜·육전(전통주 주점 추천, blog 86), 족발·광어회(시음 후기). 인용은 검색 결과 요약문에서 120자 이내. 블랙·향온·세종소주·백경53은 음식이 적힌 근거를 찾지 못해 맛 분석으로만 채웠다(블로그 본문은 자동으로 열 수 없어 요약문만 확인 — 사람이 본문을 보면 더 붙일 수 있다).
- **맛 프로필**: 출처의 묘사(당도 브릭스, "가장 가볍다/무겁다", 감미·점도)를 1~5로 옮긴 사람 판단값 — 시음 확인 전 추정.
- **남은 것**: 탁주 라인(백경10·백경13 화이트·백경 복숭아 등)은 요청 범위 밖이라 넣지 않았다. 백경12 블랙·블루, 백경 그린, 백경15 세종·조선 등 한정·신제품은 도수·판매 여부가 확인되지 않아 제외.
- 함께 고친 것: `/drinks`에서 조건에 맞는 술이 없는 지역(예: 잘못된 지역 id)에 종류를 붙여 열면 오류 나던 문제(종류 탭 안내 문구가 빈 값에 조사를 붙임).

## 6. 명령 요약

| 명령 | 용도 |
|---|---|
| `pnpm db:template` | 엑셀 템플릿 재생성 (술 목록은 시도별 묶음) |
| `pnpm --filter @pairinggo/db regional` | 전통주 지역별 목록 엑셀 재생성 (research/ 원본 기준) |
| `pnpm db:import <파일>` | 엑셀/CSV → 후보 큐 |
| `pnpm db:collect [--top N\|--drink ids\|--all] [소스 플래그] [--dry]` | 자동 수집 → 후보 큐 |
| `pnpm --filter @pairinggo/db pilot [--dry]` | `pilot/hits.json` 재적재 |
| `pnpm db:status` | 카탈로그·후보·로그 수, 마이그레이션, RLS |
| `pnpm --filter @pairinggo/db mentions:insta <csv> [--day]` | 인스타그램 해시태그 게시물 수 수동 입력(§5-2) |
| `pnpm --filter @pairinggo/db photo-request [--top N]` | 사진 요청 명단 엑셀(docs/16) |
| `pnpm --filter @pairinggo/db images <csv> [--dry]` | 허락받은 술 사진 등록(docs/16) |
| `pnpm --filter @pairinggo/db blog-counts [--all] [--dry]` | 페어링 대중 언급 수(blog_count) 채우기 — 네이버 블로그 검색 결과 수, max("술 별칭 음식", "술 이름 음식"). 기본은 0인 행만. 새 승격은 어드민이 자동으로 센다 |
| `pnpm --filter @pairinggo/db shop-insight [--fresh|--blog]` | 네이버 쇼핑인사이트 수요 측정(앱에 없는 후보 + 현재 라인업) → `research/shop-insight.json`, `--blog`는 블로그 교차 확인(§5-4) |
| `pnpm --filter @pairinggo/db lineup [--apply]` | 라인업 확장 선정·보고서(`templates/전통주_라인업_확장.xlsx`), `--apply`로 DB 추가+발행 → 이어서 `blog-counts`·`db:export`(§5-4) |
| `pnpm --filter @pairinggo/db add-drinks <json> [--apply]` | 사람이 확인한 양조장 라인 추가(`research/additions/*.json`, 근거 URL·인용 120자 검사, 모자란 페어링은 맛 분석으로 채움) → 이어서 `blog-counts`·`db:export`(§5-5) |
| `pnpm db:export` | DB → `packages/shared/data/pairings.json` (번들 폴백 갱신, 커밋) |
